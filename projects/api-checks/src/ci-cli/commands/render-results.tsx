import { Text } from "ink";
import React from "react";
import { Result } from "../../sdk/types";

export function RenderCheckResults(props: { results: Result[] }) {
  return <Text color="black"></Text>;
}
