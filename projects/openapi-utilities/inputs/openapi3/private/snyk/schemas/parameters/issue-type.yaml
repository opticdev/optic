IssueType:
  name: type
  in: query
  required: false
  description: Issue type(s) to match, comma-separated
  style: form
  explode: false
  schema:
    type: array
    items: { $ref: '../shared/issue-type.yaml#/IssueType' }

