export const DEFAULT_CONTEXT_PATH = './ci-context.json';
export const SUPPORTED_GIT_PROVIDERS = ['github', 'gitlab'] as const;
export const SUPPORTED_GITHUB_CI_PROVIDERS = ['github', 'circleci'] as const;
export const SUPPORTED_GITLAB_CI_PROVIDERS = ['gitlab'] as const;
