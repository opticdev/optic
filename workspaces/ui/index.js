// This module exists so when this package is installed as a dependency, the consuming code can know where it is located on the filesystem and use the assets in the build folder
module.exports = {
  basePath: __dirname
};